package fr.formation.blog.persistance;

/**
 * Constantes pour les requêtes JPQL (Java Persistence Query Language).
 */
public class JpqlQueries {

	/**
	 * Equivalent de "SELECT * FROM article".
	 */
	public static final String READL_ALL_ARTICLE = "SELECT a FROM Article a";
}
